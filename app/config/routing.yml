json:
    resource: "@JsonBundle/Controller/"
    type:     annotation
    prefix:   /json

relatorio:
    resource: "@RelatorioBundle/Controller"
    prefix:   /relatorio

atividade:
    resource: "@AtividadeBundle/Controller/"
    prefix:   /atividade

admin:
    resource: "@AdminBundle/Controller/"
    type:     annotation
    prefix:   /admin

app:
    resource: '@AppBundle/Controller/'
    type: annotation

logout:
    path: /logout

fos_js_routing:
    resource: "@FOSJsRoutingBundle/Resources/config/routing/routing.xml"
    prefix: /

json_localiza_cpf:
    path: /admin/usuario/jsonlocalizacpf/{cpf}
    defaults:  { _controller: AdminBundle:Usuario:jsonlocalizacpf }
