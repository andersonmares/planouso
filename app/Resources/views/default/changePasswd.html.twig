{% extends 'base.html.twig' %}

{% block body %}
    <div class="box box-success box-solid">
        <div class="box-header with-border">
            <h3 class="box-title"><span class="fa fa-tasks"></span> Alterar Senha</h3>
        </div>
        <div class="box-body">
            <div class=" col-lg-2">
                    {{ form_start(form) }}

                    {{ form_widget(form) }}

                    <button type="submit" class="btn btn-success">Salvar</button>
                    <a class="btn btn-info" href="{{ path('atividade') }}">Voltar</a>
                    {{ form_end(form)}}
            </div>
        </div>
    </div>
{% endblock %}
{% block javascripts %}
<script>

</script>
{% endblock %}